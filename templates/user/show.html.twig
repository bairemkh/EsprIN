{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <h1>User</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Cinuser</th>
                <td>{{ user.cinuser }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Passwd</th>
                <td>{{ user.passwd }}</td>
            </tr>
            <tr>
                <th>Createdat</th>
                <td>{{ user.createdat ? user.createdat|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Imgurl</th>
                <td>{{ user.imgurl }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ user.firstname }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ user.lastname }}</td>
            </tr>
            <tr>
                <th>Domaine</th>
                <td>{{ user.domaine }}</td>
            </tr>
            <tr>
                <th>Departement</th>
                <td>{{ user.departement }}</td>
            </tr>
            <tr>
                <th>Typeclub</th>
                <td>{{ user.typeclub }}</td>
            </tr>
            <tr>
                <th>Class</th>
                <td>{{ user.class }}</td>
            </tr>
            <tr>
                <th>Localisation</th>
                <td>{{ user.localisation }}</td>
            </tr>
            <tr>
                <th>Entreprisename</th>
                <td>{{ user.entreprisename }}</td>
            </tr>
            <tr>
                <th>Role</th>
                <td>{{ user.role }}</td>
            </tr>
            <tr>
                <th>State</th>
                <td>{{ user.state }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_user_index') }}">back to list</a>

    <a href="{{ path('app_user_edit', {'cinuser': user.cinuser}) }}">edit</a>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
