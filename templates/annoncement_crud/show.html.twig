{% extends 'base.html.twig' %}

{% block title %}Annoncement{% endblock %}

{% block body %}
    <h1>Annoncement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idann</th>
                <td>{{ annoncement.idann }}</td>
            </tr>
            <tr>
                <th>Subject</th>
                <td>{{ annoncement.subject }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ annoncement.content }}</td>
            </tr>
            <tr>
                <th>Destination</th>
                <td>{{ annoncement.destination }}</td>
            </tr>
            <tr>
                <th>Createdat</th>
                <td>{{ annoncement.createdat ? annoncement.createdat|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Catann</th>
                <td>{{ annoncement.catann }}</td>
            </tr>
            <tr>
                <th>State</th>
                <td>{{ annoncement.state }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_annoncement_crud_index') }}">back to list</a>

    <a href="{{ path('app_annoncement_crud_edit', {'idann': annoncement.idann}) }}">edit</a>

    {{ include('annoncement_crud/_delete_form.html.twig') }}
{% endblock %}
